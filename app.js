const book = {
name,
id,
new,
authors,
description,
number of stock,
available


}
const user = {
    name,
    role,
    email,
    password
}

const userBook = {
    bookid,
    userid,
    issueDate,
    expiredDate
}